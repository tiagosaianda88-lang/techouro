name: AI News Aggregator

on:
  push:
    branches:
      - main
  schedule:
    # Runs every single day at 00:00 UTC
    - cron: '0 0 * * *'
  # Allows you to run this workflow manually from the Actions tab
  workflow_dispatch:

jobs:
  update-news:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    
    steps:
    - name: Checkout Repository
      uses: actions/checkout@v4

    - name: Set up Python
      uses: actions/setup-python@v5
      with:
        python-version: '3.10'
        
    - name: Install Dependencies
      run: |
        python -m pip install --upgrade pip
        pip install feedparser google-genai beautifulsoup4 pypdf

    - name: Run News Pipeline Safety Tests
      env:
        PYTHONPATH: scripts
      run: python -m unittest scripts/test_update_news.py -v

    - name: Run AI News and Country Scripts
      env:
        GEMINI_API_KEY: ${{ secrets.GEMINI_API_KEY }}
      run: |
        python scripts/update_news.py
        python scripts/update_countries.py

    - name: Commit and Push Changes
      run: |
        git config --local user.email "action@github.com"
        git config --local user.name "GitHub Action AI"
        git add noticias.html paises.html index.html economia.html desporto.html tech.html geopolitica.html mercados.html ouro.html conteudos/news-archive.json
        # Only commit if there are changes
        git diff --quiet && git diff --staged --quiet || (git commit -m "Automated AI News and Country Updates" && git push)
